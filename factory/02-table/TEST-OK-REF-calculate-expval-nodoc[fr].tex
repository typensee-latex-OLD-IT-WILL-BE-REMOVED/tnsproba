\documentclass[12pt,a4paper]{article}

\makeatletter
    \input{../config/header[fr].sty}
    \input{../01-general/01-general.sty}

    \usepackage{01-calculate-expval}
\makeatother


%\usepackage{titlesec}
%\newcommand{\sectionbreak}{\clearpage}


% == EXTRAS == %


\begin{document}

\section{Les références}

\subsection{name = testone}

\calcexpval[name = testone,
            X = Y, k = i, xk = y, pk = q]{
    0      & 1   & 2   & 3    & 4    & 5   & 6   \\
    0.2000 & 0.1 & 0.2 & 0.05 & 0.15 & 0.1 & 0.2
}


\subsection{name = test/one/path}

\calcexpval[name = test/one/path,
            X = Y, k = i, xk = y, pk = q]{
    0      & 1   & 2   & 3    \\
    0.2000 & 0.1 & 0.2 & 0.05
}


\subsection{name = basic}

\calcexpval[name = basic,
            X = Y, k = i, xk = y, pk = q]{
    0      & 1   \\
    0.2000 & 0.8
}


% ---------------------- %


\section{Réutilisation}

\subsection{reuse = testone (all)}

\calcexpval[disp  = all,
            reuse = testone]{}


\subsection{reuse = test/one/path (exp, k = k, pk = p, ope = \macro{times})}

\calcexpval[disp  = exp,
            reuse = test/one/path,
            k = k, pk = p,
            ope = \times]{}


\subsection{reuse = basic (eval)}

\calcexpval[disp  = eval,
            reuse = basic]{}

\end{document}

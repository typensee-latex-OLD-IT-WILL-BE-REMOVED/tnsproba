==========
2020-09-   ????
==========

**New minor version version::``0.9.0-beta``:** see the new important features below and before.



?????? eperance cas fini via un tableau

	+ mode de base : tableau seul puis option
	 	---> exp calc
		---> deco decore
		---> cycle des couleurs


	+ donner accès via macro juste à sigma et somme développé pour inserion où l'on veut !!!

	+ mef du taleau : pour variance on passe just par ajout des carrés des écarts (car à la main c'est le plus simple)

	+ même code peut aussi juste produit la formule de calcul initial

	+ les deux effets en mpeme temps avec déco pédagogique (on entoure les celulles utilis pour calculer esp ou bien var)


calcexpval

	---> macro pas env car plus pratuique

	---> options

		+ par défaut disp = table (déf avec juste tab)

		+ disp = none : def pour usage après

		+ disp = all : détails avec les cadres (tab + formules)

		+ disp = exp : détails des calculs msi sans tableau décorés

		+ disp = formal : juste la somme sigam seul pour usage où l'on veut

		+ disp = eval : juste somme des prod pour usage où l'on veut

		+ nosigma (false par défaut) : tout sauf écriture via sigma


		+ choix de x , p et aussi de l'indice k possible via xk = , pk = , k =

		+ cycle des couelurs

		+ choix cdot ou time possible


		+ com : texte entre tableau et les calculs qui sera vide par défaut





		+ ??? name = ... : nom pour usage ultérieur

		+ ??? reuse = ... : juste les valeurs pour e moment sont réutiliése
